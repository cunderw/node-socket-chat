export const debug = (message: string, ...params: unknown[]) =>
  console.debug(message, ...params)
export const info = (message: string, ...params: unknown[]) =>
  console.info(message, ...params)
export const warn = (message: string, ...params: unknown[]) =>
  console.warn(message, ...params)
export const error = (message: string, ...params: unknown[]) =>
  console.error(message, ...params)
